Метод Evaluate - не что иное как булева функция (метод класса). 
и его декларация (не знаю что Вы вкладываете в это слово , предположим что это объявление метода) довольно прозрачно:

virtual bool Evaluate(const Date& date, const string& event) const = 0;

в классах наследниках этот метод переопределяется в зависимости от класса.
например если рассматриваете класс class DateComparisonNode - то в нем данный метод производит сравнение дат - сравнивает date и соотв. поле класса
( например return date < this->date); в классе событий - сравниваются события.

Касаемо логических операций - там производится логическое сравнение, например

if (LogicalOperation::And)

return left_->Evaluate(date, event) && right_->Evaluate(date, event);

